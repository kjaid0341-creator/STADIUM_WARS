# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Configuration (Change these in production)
JWT_SECRET=stadium_iq_access_secret_2026_super_secure
JWT_REFRESH_SECRET=stadium_iq_refresh_secret_2026_super_secure

# AI Layer API Keys (Optional — if blank, StadiumIQ uses its built-in rule-based GenAI Mock Engine)
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Force Mock AI mode for local testing
USE_MOCK_AI=false
